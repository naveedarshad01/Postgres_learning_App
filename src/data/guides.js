export const schemaGroups={Commerce:['countries','categories','suppliers','customers','addresses','products','orders','order_items','payments','shipments','returns','warehouses','inventory'],People:['departments','employees','projects','employee_projects','timesheets'],Telecom:['regions','sites','cells','cell_metrics','incidents'],Growth:['subscriptions','events']};
export const tableDescriptions={countries:'Country and geographic region lookup.',categories:'Product categories, including a parent-child hierarchy.',suppliers:'Supplier location and lead time.',customers:'Customer profile, segment, referrals and JSON preferences.',addresses:'Billing and shipping addresses; some postcodes are missing.',products:'Current catalog prices and variable JSON attributes.',orders:'One row per order; 2025 dates across four statuses.',order_items:'Three lines per order, with sale-time price and discount.',payments:'One captured payment per shipped or delivered order.',shipments:'Shipping dates and nullable delivery dates.',returns:'Synthetic item returns with a one-unit refund.',warehouses:'Three physical inventory locations.',inventory:'Product stock at each warehouse; composite key.',departments:'Department names and annual budgets.',employees:'Staff, salary and a self-referencing manager key.',projects:'Project ownership, dates and budgets.',employee_projects:'Many-to-many employee allocations.',timesheets:'Hours worked by employee, project and day.',regions:'Five operational regions.',sites:'Network sites and geographic coordinates.',cells:'600 LTE/5G cells across 100 sites.',cell_metrics:'18,000 cell-day observations in November 2025.',incidents:'Network incidents with open and closed timestamps.',subscriptions:'Start/end boundaries and monthly fees.',events:'User events, anonymous visitors and JSON metadata.',order_totals:'A live view of net item totals at order grain.'};
export const guides={
 pgadmin:[
  ['Install the database server','Install a supported PostgreSQL release (16 or later) from postgresql.org/download. pgAdmin is a client; it needs the PostgreSQL server too. Keep the port (usually 5432), username (often postgres) and the password you choose during installation.'],
  ['Register your local server','Open pgAdmin. If the server is not already listed, right-click Servers → Register → Server. Name it Local PostgreSQL. Under Connection use host localhost, port 5432, maintenance database postgres, and your installation username/password.'],
  ['Create an empty practice database','Right-click Databases → Create → Database. Name it pg_workbench and save. Alternatively open Query Tool on postgres and run CREATE DATABASE pg_workbench; by itself, outside any transaction.'],
  ['Load the practice dataset','Open Query Tool on pg_workbench. Download academy.sql below, open it in the editor, and execute the entire script. Do not use the Restore dialog: this is plain SQL, not a custom-format backup. It creates academy and lab schemas.'],
  ['Verify and begin','Refresh Schemas and run the verification query below. You should see 10,000 orders, 30,000 order items and 600 cells. Open an exercise in the app, copy its task or SQL, and run it in Query Tool. Use academy.table_name explicitly.']
 ],
 dbeaver:[
  ['Install PostgreSQL separately','Install a PostgreSQL server (16+) before connecting. DBeaver Community is a database client and does not create a PostgreSQL server on its own. Start the database service.'],
  ['Create a connection','In DBeaver choose Database → New Database Connection → PostgreSQL. Enter host localhost, port 5432, database postgres and your installation username/password. Accept the PostgreSQL driver download if requested, then Test Connection.'],
  ['Create the database','Open SQL Editor on the postgres connection with auto-commit enabled. Run CREATE DATABASE pg_workbench; as a single statement outside a transaction. Edit or duplicate the connection so its Database field is pg_workbench, then reconnect.'],
  ['Run the SQL script','Download academy.sql. Open it in SQL Editor and confirm the active connection is pg_workbench. Choose Execute SQL Script for the full file; executing only the current statement will not load the full dataset.'],
  ['Browse and practice','Refresh the connection, expand schemas academy and lab, and inspect tables or the built-in ER diagram. Run the verification query below. Copy exercise SQL into this connection; use lab for your design tables.']
 ],
 docker:[
  ['Optional server with Docker','Use this route if Docker Desktop or Docker Engine is already installed. Copy .env.example to .env and choose a local practice password. Keep that file private.'],
  ['Start the supplied service','From the extracted application folder run docker compose up -d. The compose file starts PostgreSQL 16 on localhost:5433 and loads academy.sql into a fresh pg_workbench database. Initial startup takes a few moments.'],
  ['Connect your preferred client','In pgAdmin or DBeaver use host localhost, port 5433, database pg_workbench, user learner and the password from .env. The alternate port avoids a common collision with a native PostgreSQL installation.'],
  ['Keep your database','docker compose stop stops the service without deleting the named volume. docker compose up -d starts it again. Do not remove the volume unless you intend to erase this practice database. Initial SQL scripts run only when the volume is empty.']
 ]
};
export const verificationSQL="SELECT (SELECT count(*) FROM academy.orders) AS orders,\n       (SELECT count(*) FROM academy.order_items) AS order_items,\n       (SELECT count(*) FROM academy.cells) AS cells;";
export const localDrills=[
 {id:'locking',title:'Observe a row lock with two connections',steps:['In connection A, run BEGIN; UPDATE academy.inventory SET quantity=quantity+1 WHERE warehouse_id=1 AND product_id=1; Leave the transaction open.','In connection B, run SET lock_timeout=\'3s\'; followed by the same UPDATE. Observe the lock timeout rather than assuming the server has crashed.','ROLLBACK connection A. Repeat the update in B: it should now succeed. Undo the increment if you want to keep the original data.','Explain why this needs two independent connections and how consistent lock ordering reduces deadlocks.']},
 {id:'recovery',title:'Back up and restore into a separate database',steps:['Run pg_dump -h localhost -U postgres -Fc -f pg_workbench.dump pg_workbench using the appropriate user/port for your installation.','Create a different empty database: createdb -h localhost -U postgres pg_workbench_restore.','Restore with pg_restore -h localhost -U postgres -d pg_workbench_restore pg_workbench.dump. Do not overwrite your working database.','Run the verification query on the restored database. Compare table counts and a known report total. Record how long recovery took.']},
 {id:'roles',title:'Prove a reporting role is read-only',steps:['As an administrator in your dedicated practice database, create role report_reader NOLOGIN. Grant USAGE on schema academy and SELECT on all its current tables.','Run BEGIN; SET LOCAL ROLE report_reader; then SELECT count(*) FROM academy.orders; It should succeed.','Try an INSERT into academy.orders. It should fail with insufficient privilege. ROLLBACK to clear the aborted transaction.','Explain why new tables need default privileges configured for the role that creates them. Do not use the administrator account in a real application.']},
 {id:'plans',title:'Measure a query plan before and after indexing',steps:['Run EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM academy.orders WHERE order_date>=DATE \'2025-11-01\' AND order_date<DATE \'2025-12-01\'; Record the plan.','Create an index on academy.orders(order_date), run ANALYZE academy.orders, then run the same EXPLAIN again.','Compare actual/estimated rows, scan choice, buffers and execution time. The planner may legitimately prefer a sequential scan.','Document the data size and why a 10,000-order result does not prove the same improvement at production scale.']},
 {id:'portfolio-commerce',title:'Publish a reproducible commerce analysis locally',steps:['Save the module 29 queries and document date window, status exclusions, currency and metric grain.','Add at least three checks: duplicate keys, payment reconciliation and explicit NULL handling.','Write a one-page interpretation of the results with two business questions to investigate next.','Package your SQL and README so another person can load the provided seed and reproduce every number.']},
 {id:'portfolio-telecom',title:'Deliver a network KPI investigation',steps:['Save module 28 SQL with the PRB, throughput and availability thresholds clearly stated.','Add a calendar-based data completeness check and compare cell-day counts by region.','Explain which cells should be investigated, which evidence is missing, and why correlation does not prove the root cause.','Include a schema description, query plan and a short validation note with your analysis.']}
];
export const quiz=[
 ['Which expression finds missing delivery dates?',['delivered_at = NULL','delivered_at IS NULL','delivered_at == NULL','delivered_at IN (NULL)'],1,'Use IS NULL; ordinary equality with NULL evaluates to UNKNOWN.'],
 ['Which COUNT gives zero for unmatched orders after a customer LEFT JOIN?',['COUNT(*)','COUNT(customer_id)','COUNT(o.order_id)','COUNT(1)'],2,'Count a non-null key from the right side, not the preserved outer row.'],
 ['Which clause filters groups after aggregation?',['WHERE','HAVING','LIMIT','ON'],1,'HAVING can test aggregate results.'],
 ['What result can 5 / 2 produce when both operands are PostgreSQL integers?',['2','2.5','3','NULL'],0,'Integer division truncates. Cast an operand to numeric for a fractional answer.'],
 ['Which operation preserves duplicate result rows?',['UNION','UNION ALL','INTERSECT','EXCEPT'],1,'UNION ALL appends without duplicate elimination.'],
 ['Which ranking for 100,100,90 is DENSE_RANK descending?',['1,2,3','1,1,3','1,1,2','0,0,1'],2,'DENSE_RANK does not leave a gap after ties.'],
 ['Which JSONB operator extracts text?',['->','->>','@>','?'],1,'->> returns text; -> returns a JSON value.'],
 ['CHECK(amount > 0) without NOT NULL does what with NULL?',['Always rejects it','Allows it','Changes it to zero','Raises a syntax error'],1,'A CHECK permits TRUE or UNKNOWN; NOT NULL is a separate constraint.'],
 ['An order joins three items and two independent tags. How many joined rows can result?',['2','3','5','6'],3,'The one-to-many matches multiply: three times two.'],
 ['What does EXPLAIN ANALYZE do?',['Only estimates a query','Executes the statement and measures work','Creates an index','Updates all statistics'],1,'ANALYZE in EXPLAIN executes the statement, including writes.'],
 ['What is required for a reliable next-page keyset query?',['No ORDER BY','A stable ordering and continuation key','An increasing OFFSET only','SELECT DISTINCT on every column'],1,'Use the same stable sort keys in the predicate and ORDER BY.'],
 ['Which stores the price actually agreed at purchase time?',['Only products.price','order_items.unit_price','Current category name','The average product price'],1,'Historical sale price is a transaction fact.'],
 ['Which is normally a good PostgreSQL type for exact monetary calculations?',['real','double precision','numeric','text'],2,'Numeric supports exact decimal arithmetic; scale and rounding still need a business rule.'],
 ['After an error inside BEGIN, what normally restores a usable transaction state?',['Ignore the error','SELECT 1','ROLLBACK or ROLLBACK TO a prior savepoint','Another INSERT'],2,'PostgreSQL leaves the transaction aborted until rollback.'],
 ['A materialized view is best described as what?',['Always current','A stored result that needs refresh','An automatic backup','An index alias'],1,'Freshness depends on its refresh strategy.'],
 ['Which avoids NOT IN problems caused by NULL in the subquery?',['NOT EXISTS with a correlated key','Adding LIMIT 1','COUNT(*)','ORDER BY'],0,'NOT EXISTS tests matching-row existence without NULL poisoning.'],
 ['Can a table owner normally bypass enabled row-level security?',['Yes, unless forced','No, never','Only in a view','Only after VACUUM'],0,'Owners normally bypass RLS; test with the actual restricted role.'],
 ['Which month filter is half-open?',["d BETWEEN '2025-11-01' AND '2025-12-01'","d >= '2025-11-01' AND d < '2025-12-01'","d > '2025-11-01'","d <= '2025-12-01'"],1,'Include the beginning and exclude the next period boundary.'],
 ['What is the safest way to pass an untrusted value to an application query?',['Concatenate it into SQL','Remove spaces','Use a bound query parameter','Convert it to uppercase'],2,'Bound parameters keep values separate from the SQL syntax.'],
 ['What best proves a backup is useful?',['The dump file exists','The file is large','A successful restore with validation','A green dashboard'],2,'Restore into a separate database and validate expected data.']
].map((x,i)=>({id:i+1,question:x[0],options:x[1],answer:x[2],explanation:x[3]}));
